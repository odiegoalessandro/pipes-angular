import { ArrayPipeImpurePipe } from './array-pipe-impure.pipe';

describe('ArrayPipeImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayPipeImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
