import { ArrayPipePipe } from './array-pipe.pipe';

describe('ArrayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
